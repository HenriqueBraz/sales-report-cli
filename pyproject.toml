[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sales-report-cli"
version = "0.1.0"
description = "Advanced sales report generator"
requires-python = ">=3.10"